#include <stdio.h>
/**
 * main - the entry point
 *
 * Description: Here, the main function
 * will uses printf function to print
 * some formatted string
 *
 * Return: it returns zero
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,");
	return (0);
}
